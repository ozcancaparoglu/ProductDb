@inject CustomLocalizer localizer
@model CurrencyModel
@using (Html.BeginForm("Create", "Currency", FormMethod.Post, new { enctype = "multipart/form-data" }))
{
    <!-- Content Header (Page header) -->
    <section class="content-header clearfix">
        <h1 class="pull-left">
            @localizer["Currency"]
            <small>@localizer["Create"]</small>
        </h1>

        <div class="pull-right">
            <button type="submit" name="save" class="btn bg-blue">
                <i class="fa fa-floppy-o"></i>
                @localizer["Save"]
            </button>
            <a href="/@localizer.CurrentLanguage/currency/list" class="btn bg-red">
                <i class="fa fa-ban"></i>
                @localizer["Cancel"]
            </a>
        </div>
    </section>

    <section class="content">
        <div class="row">
            <div class="box box-primary">
                <div class="box-header with-border">
                    <h3 class="box-title">@localizer["AddNew"] - @localizer["Currency"]</h3>
                </div>
                <div class="box-body">
                    <div class="row">
                        <div class="col-md-6">
                            <div class="panel panel-default">
                                <div class="panel-heading">
                                    @localizer["GeneralInformation"]
                                </div>
                                <div class="panel-body">
                                    <div class="form-group">
                                        <label for="CurrencyName">@localizer["Name"]</label>
                                        @Html.TextBoxFor(model => model.Name, new { @class = "form-control", placeholder = "Enter currency name" })
                                        <span class="text-red">@Html.ValidationMessageFor(model => model.Name)</span>
                                    </div>
                                    <div class="form-group">
                                        <label for="CurrencyAbbrevation">@localizer["Abbrevation"]</label>
                                        @Html.TextBoxFor(model => model.Abbrevation, new { @class = "form-control", placeholder = "Enter abbrevation" })
                                        <span class="text-red">@Html.ValidationMessageFor(model => model.Abbrevation)</span>
                                    </div>
                                    <div class="form-group">
                                        <label for="CurrencyValue">@localizer["Value"]</label>
                                        @Html.TextBoxFor(model => model.Value, new { @class = "form-control", placeholder = "Enter value" })
                                        <span class="text-red">@Html.ValidationMessageFor(model => model.Value)</span>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>
}
